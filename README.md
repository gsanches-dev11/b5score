# b5score

Descrição
Descrição curta do projeto (substitua por uma frase sucinta que explique o propósito do projeto).

Status
- Em desenvolvimento

Tecnologias
- Java
- React
- CSS

Instalação
1. Clone o repositório: `git clone https://github.com/gsanches-dev11/b5score.git`
2. Siga as instruções específicas do backend e do frontend (ex.: `mvnw package` / `npm install`).

Uso
- Instruções básicas para executar o projeto localmente:
  - Backend: (ex.: `./mvnw spring-boot:run` ou `./gradlew bootRun`)
  - Frontend: (ex.: `npm install` e `npm start` dentro da pasta do frontend)

Testes
- Rodar a suíte de testes (se houver): `mvn test` / `npm test` (substitua conforme sua configuração).

Contribuição
- Abra issues para discutir features antes de PRs grandes.
- Prefiro PRs pequenos, com descrição clara e testes quando aplicável.

Licença
Este projeto está licenciado sob a MIT License — veja o arquivo LICENSE.